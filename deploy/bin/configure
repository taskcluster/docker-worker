#! /usr/bin/env node
var spawn = require('child_process').spawn;

var proc = spawn(
  'node',
  ['--harmony', __dirname + '/configure.js'],
  { stdio: 'inherit' }
);

proc.once('exit', process.exit);
